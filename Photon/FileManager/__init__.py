from .Stream import Stream
from .FileManager import FileManager, File, FileOpener

PHOTON_FILEMANAGER_VERSION = (1,0,0)
PHOTON_FILEMANAGER_VERSION_STR = "".join([str(x) for x in PHOTON_FILEMANAGER_VERSION])
